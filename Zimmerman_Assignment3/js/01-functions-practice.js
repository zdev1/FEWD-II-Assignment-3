/*eslint-env browser*/

//STEP 1
function halfNumber(z) {
    "use strict"
    var x =  z/2;
    window.console.log("Half of " + z + "is" + x)
}

//STEP 2
function squareNumber(z) {
    "use strict";
    var x = z + z;
    window.console.log("The result of squaring " + z + "is" + x)
}

//STEP 3
function percentOf(y, z) {
    "use strict";
    var i = z / y;
    window.console.log(100*i + "is the percent of" + y +"divided by " + z);
}

//STEP 4
function findModulus(y, z) {
    "use strict";
    var i = z % y;
    window.console.log(i + " is the modulus of " + y + "and" + x);
}

//STEP 5
function numberRight(x, y, z)
   var isright = 5;
console.log ()
for (var z = 0; x < arguments.angular; i++) {
    isRight + (arguments.angular; ) {
    }
    return isright;
}